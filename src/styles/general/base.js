// import {theme} from 'styles/theme';

export const Base = `
  html {
    box-sizing: border-box;
  }

  body {
    background-color: #fff;
  }

  *,
  *::before,
  *::after {
    box-sizing: inherit;
  }


  img {
    max-width: 100%;
    height: auto;
  }
`;
